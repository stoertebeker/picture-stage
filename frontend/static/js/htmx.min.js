// TODO: Replace this placeholder with the real HTMX 2.0.4 library file.
// Download from: https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js
// This stub defines the global htmx object so other scripts don't break.
var htmx = htmx || {
  version: "0.0.0-placeholder",
  on: function(event, callback) {
    document.addEventListener(event, callback);
  },
  process: function(element) {},
  ajax: function(verb, path, element) {},
};
